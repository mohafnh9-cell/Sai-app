import type { BenchmarkCase } from "../../types";

export const INJECTION_NEGATIVE_CASES: BenchmarkCase[] = [
  {
    id: "injection.sql-negative-01",
    ruleId: "injection.sql",
    expected: "no_detect",
    category: "injection",
    language: "typescript",
    kind: "negative",
    source: "benchmark-new",
    description: "Parameterized query with a placeholder, no string interpolation or concatenation.",
    files: [{ path: "server/db.ts", content: "pool.query('SELECT * FROM accounts WHERE user_id = $1', [userId]);" }],
  },
  {
    id: "injection.command-negative-01",
    ruleId: "injection.command",
    expected: "no_detect",
    category: "injection",
    language: "typescript",
    kind: "negative",
    source: "benchmark-new",
    description: "execFile with a fixed command and an explicit argument array, no request data or shell.",
    files: [{ path: "server/convert.ts", content: "execFile('convert', ['--version']);" }],
  },
  {
    id: "injection.path-traversal-negative-01",
    ruleId: "injection.path-traversal",
    expected: "no_detect",
    category: "injection",
    language: "typescript",
    kind: "negative",
    source: "benchmark-new",
    description: "A filesystem read of a fixed, hardcoded path.",
    files: [{ path: "server/config-loader.ts", content: "readFileSync('./config/defaults.json');" }],
  },
  {
    id: "injection.ssrf-negative-01",
    ruleId: "injection.ssrf",
    expected: "no_detect",
    category: "injection",
    language: "typescript",
    kind: "negative",
    source: "benchmark-new",
    description: "Server-side outbound fetch to a fixed, hardcoded allowlisted host.",
    files: [{ path: "server/status-check.ts", content: "await fetch('https://status.stripe.com/api/v2/status.json');" }],
  },
  {
    id: "injection.deserialization-negative-01",
    ruleId: "injection.deserialization",
    expected: "no_detect",
    category: "injection",
    language: "typescript",
    kind: "negative",
    source: "benchmark-new",
    description: "Structured JSON parsing, never eval/new Function/unserialize.",
    files: [{ path: "server/parse-payload.ts", content: "const result = JSON.parse(payload);" }],
  },
];
