import type { BenchmarkCase } from "../../types";

export const INJECTION_POSITIVE_CASES: BenchmarkCase[] = [
  {
    id: "injection.sql-positive-01",
    ruleId: "injection.sql",
    expected: "detect",
    category: "injection",
    language: "typescript",
    severity: "high",
    kind: "positive",
    source: "existing-rule-test",
    description: "Request-derived value interpolated directly into a SQL template literal (rules.test.ts).",
    files: [{ path: "api/users.ts", content: "db.query(`SELECT * FROM users WHERE id = ${req.query.id}`)" }],
  },
  {
    id: "injection.command-positive-01",
    ruleId: "injection.command",
    expected: "detect",
    category: "injection",
    language: "typescript",
    severity: "critical",
    kind: "positive",
    source: "existing-rule-test",
    description: "exec() call whose command string interpolates a request-derived value (rules.test.ts).",
    files: [{ path: "api/convert.ts", content: "exec(`convert ${req.body.name}`)" }],
  },
  {
    id: "injection.path-traversal-positive-01",
    ruleId: "injection.path-traversal",
    expected: "detect",
    category: "injection",
    language: "typescript",
    severity: "high",
    kind: "positive",
    source: "existing-rule-test",
    description: "A filesystem read whose path comes straight from a query parameter (rules.test.ts).",
    files: [{ path: "api/files.ts", content: "readFile(req.query.path)" }],
  },
  {
    id: "injection.ssrf-positive-01",
    ruleId: "injection.ssrf",
    expected: "detect",
    category: "injection",
    language: "typescript",
    severity: "high",
    kind: "positive",
    source: "benchmark-new",
    description: "Server-side outbound fetch whose URL is built from a query parameter.",
    files: [{ path: "server/webhook-proxy.ts", content: "await fetch(`${req.query.targetUrl}`);" }],
  },
  {
    id: "injection.deserialization-positive-01",
    ruleId: "injection.deserialization",
    expected: "detect",
    category: "injection",
    language: "typescript",
    severity: "critical",
    kind: "positive",
    source: "benchmark-new",
    description: "eval() invoked on a value that could come from untrusted input.",
    files: [{ path: "server/plugin-runner.ts", content: "const result = eval(pluginSource);" }],
  },
];
