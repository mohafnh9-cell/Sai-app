import type { BenchmarkCase } from "../../types";

export const AUTH_POSITIVE_CASES: BenchmarkCase[] = [
  {
    id: "auth.missing-positive-01",
    ruleId: "auth.missing",
    expected: "detect",
    category: "auth",
    language: "typescript",
    framework: "nextjs",
    severity: "medium",
    kind: "positive",
    source: "existing-rule-test",
    description: "A mutating API route with no recognizable auth check anywhere in the file (features/security-scanner/__tests__/known-safe-patterns.test.ts).",
    files: [{ path: "app/api/unprotected/route.ts", content: "export async function POST() {\n  return Response.json({ ok: true });\n}" }],
  },
  {
    id: "auth.insecure-cookie-positive-01",
    ruleId: "auth.insecure-cookie",
    expected: "detect",
    category: "auth",
    language: "typescript",
    severity: "high",
    kind: "positive",
    source: "benchmark-new",
    description: "Cookie explicitly disables the secure flag.",
    files: [{ path: "server/session.ts", content: "response.cookie('session', token, { httpOnly: true, secure: false });" }],
  },
  {
    id: "auth.insecure-jwt-positive-01",
    ruleId: "auth.insecure-jwt",
    expected: "detect",
    category: "auth",
    language: "typescript",
    severity: "critical",
    kind: "positive",
    source: "existing-rule-test",
    description: "JWT verification explicitly accepts the 'none' algorithm (features/security-scanner/__tests__/rules.test.ts).",
    files: [{ path: "server/jwt.ts", content: "jwt.verify(token, key, { algorithms: ['none'] })" }],
  },
  {
    id: "auth.session-client-storage-positive-01",
    ruleId: "auth.session-client-storage",
    expected: "detect",
    category: "auth",
    language: "typescript",
    framework: "react",
    severity: "high",
    kind: "positive",
    source: "benchmark-new",
    description: "Auth token stored in localStorage, readable by any XSS.",
    files: [{ path: "components/AuthProvider.tsx", content: "localStorage.setItem('authToken', token);" }],
  },
];
