# RT10 (LLM Security)

Path: `server/ai-red-team/llm-team/`.

## Architecture

- **Coordinator** → declarative pipeline (`declarative/run-declarative-pipeline.ts`).
- **Manifest**: `declarative/manifest.ts` (`rt10.llm`).
- **Auto-register**: `declarative/register.ts` on import.
- **Capabilities**: `capabilities/register-llm-capabilities.ts`.
- **Platform**: `integration/` (protected assets, preconditions, Mission Control metrics).
- **Safe runtime**: `runtime/production-guard.ts` + team runtime config.

## Migration status (stabilization)

| Concern | Status |
|---------|--------|
| Declarative pipeline orchestration | ✅ Via RT-Core |
| Finding primitives | ✅ Aliased from core |
| AI discovery / execution graph / specialists | ⚠️ Domain code in RT10 (expected) |
| Legacy `runRt10FindingsPipeline` | Retained for unit tests; production uses declarative path |
| Platform integration | ✅ Slice 8 |

Feature gate: `llm_team` + `SEQURAI_LLM_TEAM_MODE` (`integration/feature-gate.ts`).
