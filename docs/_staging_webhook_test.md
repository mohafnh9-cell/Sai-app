# Staging webhook verification

This file exists only to trigger a `pull_request` event for the SequrAI
staging GitHub App webhook end-to-end test. Safe to delete after the
STAGING GO checklist item is closed.

Second commit to verify a new check is created per head SHA.
